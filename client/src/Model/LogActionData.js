const logAction = {
    user_log_succeesful: " has log into the system",
    user_log_failed: " has tries to log in to the system but failed.",
    user_register_successful:" has register in to the system"
};

export default logAction;

